/**
 * print_buffer - Prints the content of size bytes of the buffer poin ted by b
 * @b - points to a buffer
 * @size - size bytes of the outputed content
 * Return: 10 bytes per line
 */
void print_buffer(char *b, int size)
{
	printf('\n')
}
